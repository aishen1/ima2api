# ima2api — 各版本更新说明

## 安装方式

下载对应版本的 `ima2api-1.0.x.fpk`，在 fnOS 应用中心安装：

```bash
appcenter-cli install-fpk /path/to/ima2api-1.0.x.fpk
```

---

## 1.0.8 — 网页扫码登录（不用再抓包）

`ima2api-1.0.8.fpk`

**新增：配置页内嵌微信扫码登录**

配置页新增「扫码登录」区域，内嵌 ima 官方登录页，用微信扫一扫即可添加账号：

- 支持**多账号**反复扫码添加，扫完即用
- 拿到凭证后**立即发一次真实请求校验**，无效凭证不会静默入库
- 二维码可随时「刷新」（微信侧有有效期限制）
- 内嵌失败时保留「新窗口打开登录页」与「手动粘贴 Cookie」两条兜底路径

**为什么能免抓包**：微信开放平台的 `redirect_uri` 白名单**只放行 `https://ima.qq.com/login`**
（实测 `localhost`、内网 IP、自建域名全被拒），所以 `code` 不可能回调到 NAS 地址。
但 ima 登录页自身会用 `postMessage` 把微信 `code` 交给宿主页面（事件名 `loginWxCodeReady`），
本项目直接复用这一机制 —— 因此**全程不需要浏览器插件、不需要代理抓包、不需要公网**。

⚠️ **安全设计**：宿主页面只接受 **`https` + 精确域名 `ima.qq.com`** 的 `postMessage`。
只校验域名是不够的 —— `http://ima.qq.com` 会被中间人替换掉 `code`，协议必须一并校验。
（这个漏洞是在写测试时被断言抓出来的，已修复。）

**本版本同时包含 1.0.7 的全部改动**（模型列表自动同步，见下）。

**验证**：回归测试 95/95 通过 —— `isLocalRequest` 20、`qr-login` 49、`qr-postmessage` 26。
端到端实测：假 `code` 被 ima 正确拒绝（`40029 invalid code`），证明链路已通到微信侧。

---

## 1.0.7 — 模型列表与 IMA 客户端「内置模型」自动同步

> 该版本未单独出包，改动已并入 1.0.8 一起发布。

`ima2api-1.0.7.fpk` · SHA256: `bd8dba608cc5e49b10f87d01a5fa3d5a`

**背景**：模型表原先硬编码在 `server.js` 里（6 个旧模型），IMA 官方已调整过模型阵容却无从感知。

**改动**：新增免登录接口 `POST https://ima.qq.com/cgi-bin/model_manage/get_models`（就是 IMA 客户端「内置模型」调的那个），服务启动时自动拉取并**整体替换**模型表 —— 与官方接口**严格一致，不保留任何旧模型**。

- 官方返回 4 个模型，每个带 `sub_model_infos`；**思考模式是子模型，不单独占条目**，用 `-think` / `-reasoning` 后缀调用（如 `glm-5-3-flash-think`）
- 模型名解析放宽：slug（`glm-5-3-flash`）、原始显示名（`GLM-5.3-Flash`）、裸 `model_type`（`3001`）均可
- 管理页新增「同步模型」按钮 + 上次同步时间；`POST /admin/models/sync` 可编程触发
- 上游不可达时自动沿用上次模型表，服务不中断，错误写入 `models_sync_error`

**注意**：`glm-5.2` 系列在官方侧已改名为 **GLM-5.3-Flash**（同一 `model_type` 槽位 3000/3001），升级后请把客户端模型名改为 `glm-5-3-flash` / `glm-5-3-flash-think`。

**验证**：真机跑同步 —— 上游 4 个 → 本地 4 个，旧模型残留 0，`/v1/models` 输出 4 条；模型名解析 14/14 通过；上游不可达时回退正常。

---

## 1.0.6 — 飞牛 App 内嵌模式 + 调用地址固定输出

`ima2api-1.0.6.fpk` · SHA256: `042cb65a278382e4399734e62cfb5e6f`

**两个核心问题一起解决：**

1. **地址缺端口**：之前让浏览器优先计算地址，内嵌后浏览器地址是飞牛网关（`http://NAS:5666/app/ima2api/`），拿它当调用地址既带不出端口，也对调用方毫无意义。
   → 改为服务端权威：`serverBase()` 恒用「本机内网 IP + 实际监听端口」，不再采信请求 Host。无论从哪条路径打开，显示的都是同一个可直连地址，如 `http://192.168.9.102:8088/v1`。

2. **内嵌打开，不跳转、不走公网**：桌面入口改为飞牛内嵌网关（`type: iframe`），通过 unix socket 与网关通信，全程走本机，不经过公网。服务端同时监听 TCP 8088（对外 API）和 unix socket（供网关），支持路径前缀剥离，前端请求自动带前缀。进程退出时清理 socket 文件，避免残留导致 EADDRINUSE。

**其他改进**
- 管理接口来源判定改为只看 TCP 对端地址，不再采信 `X-Forwarded-For`，修复了"把自己人拦在门外"的问题，同时堵住了伪造 XFF 绕过校验的安全漏洞
- 403 响应带上来源诊断（peer / forwarded_for），页面直接把服务端实际看到的来源显示出来，不用再翻日志
- 状态接口失败时，前端仍用浏览器地址填充可用地址，并显示来源诊断

---

## 1.0.5 — 修复管理接口把自己人拦掉

`ima2api-1.0.5.fpk` · SHA256: `1589d144ac26bd3a4b2c4c270fdcd334`

**根因**：`isLocalRequest()` 写的是 `const clientIp = xff || ip`，优先采信 `X-Forwarded-For`。手机经 App 中继访问时，XFF 里写的是公网出口 IP（日志实测 `111.163.88.123`、`223.104.7.67`），而 TCP 对端其实是内网地址（`192.168.9.103`），导致内网访问被误判为公网拒绝。同一个缺陷也是安全隐患：攻击者从公网直连时伪造 `X-Forwarded-For: 192.168.x.x` 就能通过校验。

**修复**：只看 TCP 对端地址，完全忽略 XFF；新增 `normalizeIp()` 归一化 IPv4-mapped IPv6；403 响应带上来源诊断。

**验证**：后端 20/20（含"对端内网+XFF公网→放行"、"公网直连+伪造XFF→拒绝"）、前端 9/9。

---

## 1.0.4 — 调用地址改为浏览器端计算

`ima2api-1.0.4.fpk` · SHA256: `07bcb21de01fb63f2495ffb0df0aae91`

**问题**：1.0.3 的地址显示会在某些场景下只显示 `/v1` 半截值（三个字符），无法复制使用。

**修复**：地址改为浏览器端权威计算（`resolveBase()`），不再依赖服务端按请求 Host 推算；页面添加 `Cache-Control: no-store` 防止 Android WebView 缓存旧页面；注入构建版本标记（`<meta name="ima2api-build">`）便于确认实际加载版本。

---

## 1.0.3 — 调用地址显示完整 + 修复窄屏裁切

`ima2api-1.0.3.fpk` · SHA256: `（同 1.0.4 前版本，建议直接升 1.0.4+）`

- 地址字段改为可断行完整显示（不再横向裁切）
- 复制按钮添加 `execCommand` 回退，在 `http://` 局域网下也能用
- 页面在手机窄屏下不再被裁切

---

## 1.0.2 — 账号有效性检测 + 多账号支持

`ima2api-1.0.2.fpk` · SHA256: `（同 1.0.3 前版本，建议直接升最新）`

- 账号状态 6 态显示（有效/已失效/异常/缺凭证/已停用/未验证）
- 区分"凭证无效"与"网络错误"
- 支持多账号轮询 + 自动故障切换
- Cookie 自动刷新保活

---

## 1.0.0 — 首个公开发布版

`ima2api-1.0.2.fpk`（早期版本，建议直接升最新）

- 多账号支持 + 网页配置页
- 通过 Cookie 解析，支持多账号
- 本地 URL 和 Key 调用

---

## 技术架构

- **服务端**：Node.js + Express，自带运行时（v24.15.0），安装无需预装 Node.js
- **配置**：web 页面添加/管理 Cookie 账号，显示调用地址 + API Key
- **调用地址**：服务端返回内网 IP + 端口，与请求路径无关
- **内嵌模式**：支持飞牛 App iframe 内嵌，全程走本机 unix socket
- **账号池**：round-robin + 自动故障切换，支持多账号
- **自动刷新**：token 有效期 2h，提前 10 分钟刷新，无需重启服务
